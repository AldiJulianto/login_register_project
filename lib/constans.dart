//Mengimport library material.dart
import 'package:flutter/material.dart';

//Membuat class ColorPalette yang akan digunakan untuk pewarnaan halaman login dan register
class ColorPalette {
  static const primaryColor = Color(0xfffca311);
  static const buttonColor = Color(0xff14213d);
  static const underlineTextField = Color(0xff000000);
  static const hintText = Color(0xffe5e5e5);
}